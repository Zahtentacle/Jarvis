#!/usr/bin/env sh
exec "./gradle/wrapper/gradle-wrapper.properties" # Ini hanya penanda, GitHub Actions akan mendeteksi pembungkus otomatis
# Atau lebih aman, biarkan GitHub Actions yang mendownloadnya dengan perintah di build.yml
